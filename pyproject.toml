[project]
name = "inference-autoresearch"
version = "0.1.0"
description = "Autonomous inference-path benchmarking on MLX"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mlx",
    "mlx-lm",
]
