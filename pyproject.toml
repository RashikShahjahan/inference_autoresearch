[project]
name = "inference_assistant"
version = "0.1.0"
description = "Research assistant workflow for inference-path benchmarking on MLX"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "huggingface-hub",
    "mlx",
    "mlx-lm",
]
